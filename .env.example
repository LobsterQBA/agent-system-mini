# Demo mode needs no key.
# To enable Live mode, copy this file to .env and set:
AGENT_API_KEY=
AGENT_MODEL=gpt-4.1-mini
# Optional for an OpenAI-compatible endpoint:
# AGENT_BASE_URL=https://api.openai.com/v1

# Local state and server settings
AGENT_HOME=.agent-mini
AGENT_PORT=8787
